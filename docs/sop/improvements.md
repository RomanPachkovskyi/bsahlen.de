# SOP: Improvements — Lessons Learned

**Базується на:** Реальна міграція bsahlen.de (2026-01-28)
**Версія:** 2.1

---

## Огляд

Під час міграції проєкту **bsahlen.de** зі старої структури на SOP v2.0 виявлено **10 критичних недоліків та пропусків** у документації.

Цей модуль містить детальний аналіз кожної проблеми та рішення, які вже реалізовані в модульній структурі SOP v2.1.

---

## 10 Issues (Priority Order)

### 🔴 CRITICAL

1. [Відсутність інструкцій для міграції](#1-відсутність-інструкцій-для-міграції)
2. [Plesk Git setup без деталей](#3-plesk-git-setup-без-деталей)
3. [Rollback procedures відсутні](#6-rollback-procedures-відсутні)

### 🟡 HIGH

4. [Неясна обробка 3rd party плагінів](#2-неясна-обробка-3rd-party-плагінів)
5. [MODE для живих сайтів](#4-mode-для-живих-сайтів)
6. [Elementor CSS regeneration](#8-elementor-css-regeneration)
7. [Child theme paths при міграції](#9-child-theme-paths-при-міграції)

### 🟢 MEDIUM

8. [Docker mount strategy](#5-docker-mount-strategy)
9. [Language files в .gitignore](#7-language-files-в-gitignore)
10. [Staging environment strategy](#10-staging-environment-strategy)

---

## 1. Відсутність інструкцій для міграції

### Проблема

**Серйозність:** 🔴 CRITICAL

SOP v2.0 описує тільки створення НОВИХ проектів через `bootstrap.sh`. Немає жодної інформації про міграцію існуючих проектів.

**Вплив:**
- Неможливо мігрувати старі проєкти без додаткової документації
- Ризик зламати існуючі production сайти
- Відсутній rollback план

### Рішення ✅

**Реалізовано в SOP v2.1:**

1. **[migration.md](migration.md)** — новий модуль з:
   - Overview процесу міграції (10 фаз)
   - Quick Start guide
   - Детальні checklistи
   - Rollback plan
   - Common issues

2. **[docs/migration/MIGRATION.md](../migration/MIGRATION.md)** — 68-сторінковий посібник:
   - Bash команди для кожної фази
   - Troubleshooting
   - Rollback procedures

3. **Template для нових проектів:**
   ```bash
   cp docs/migration/MIGRATION.md docs/migration/MIGRATION_PLAN_[project].md
   ```

**Статус:** ✅ Повністю вирішено

---

## 2. Неясна обробка 3rd party плагінів

### Проблема

**Серйозність:** 🟡 MEDIUM

SOP каже "тільки custom-* в Git", але не пояснює:
- Як саме визначити які плагіни custom?
- Як працювати з `plugins/` якщо частина в Git, частина ні?
- Що робити з плагінами що мають customizations?

**Вплив:**
- Плутанина при розробці
- Можливі конфлікти при deploy
- Різні підходи в різних проектах

### Рішення ✅

**Реалізовано в [basics.md § 4.1](basics.md#плагіни-в-git-детальні-правила):**

```markdown
#### Плагіни в Git: Детальні правила

✅ В Git:
- wp/wp-content/plugins/custom-* - будь-які custom плагіни
- Плагіни створені студією з нуля
- Приватні плагіни (недоступні в WP repo)

❌ НЕ в Git:
- Публічні плагіни з wordpress.org
- Преміум плагіни (Elementor Pro, ACF Pro, etc.)
- Будь-які плагіни що можна встановити через WP Admin

⚠️ Особливі випадки:
- Якщо 3rd party плагін має customizations → fork + rename до custom-*
- Якщо плагін недоступний (deprecated) → обговорити з командою
```

**.gitignore strategy:**
- Явні виключення для всіх 3rd party plugins
- PROJECT.md має секцію "Plugin Management Strategy"

**Статус:** ✅ Повністю вирішено

---

## 3. Plesk Git setup без деталей

### Проблема

**Серйозність:** 🔴 HIGH

SOP згадує "Plesk auto-deploy" але немає покрокової інструкції:
- Як налаштувати Git в Plesk?
- Які саме параметри встановити?
- Як згенерувати і додати SSH keys?
- Що робити якщо щось не працює?

**Вплив:**
- Власник проєкту не може налаштувати deploy
- Ризик неправильної конфігурації
- Можливий downtime на production

### Рішення ✅

**Реалізовано в [deployment.md § 2](deployment.md#2-plesk-git-setup):**

**8 детальних кроків:**
1. Увійти в Plesk
2. Налаштувати Repository
3. **Deployment Mode** (MANUAL спочатку!)
4. SSH Keys (для приватних repo)
5. Тестовий Pull
6. Deploy Actions
7. Перший Manual Deploy
8. Переключення на Automatic (опційно)

**Кожен крок містить:**
- Точні параметри
- Screenshots location
- What to expect
- Troubleshooting

**Статус:** ✅ Повністю вирішено

---

## 4. MODE для живих сайтів

### Проблема

**Серйозність:** 🟡 MEDIUM

SOP каже що `MODE='maintenance'` за замовчуванням для нових проектів. Але що якщо сайт УЖЕ живий і мігрується на SOP v2.0?

**Вплив:**
- Ризик випадкового вимкнення живого сайту
- Плутанина з default MODE
- Можливий SEO impact (якщо сайт показує maintenance)

### Рішення ✅

**Реалізовано в [deployment.md § 3.2](deployment.md#32-default-mode-залежно-від-project-state):**

```markdown
### Default MODE залежно від Project State

Нові проєкти (bootstrap.sh):
- Default: MODE = 'maintenance'
- Причина: сайт ще не готовий до публікації

Існуючі проєкти (міграція):
- Якщо сайт живий → MODE = 'live'
- Якщо сайт в розробці → MODE = 'maintenance'

⚠️ ВАЖЛИВО:
При міграції живого сайту завжди встановлюй:
define('MODE', 'live'); // НЕ 'maintenance'!
```

**Checklist додано:**
- PROJECT.md має поле "Default MODE"
- Перевірка перед deploy

**Статус:** ✅ Повністю вирішено

---

## 5. Docker mount strategy

### Проблема

**Серйозність:** 🟢 MEDIUM

Bootstrap.sh монтує тільки `/wp/wp-content`, але при міграції безпечніше монтувати весь `/wp` (щоб WP Core теж був локально).

**Вплив:**
- Плутанина з mount strategy
- Можливі проблеми з permissions
- Складніше дебажити

### Рішення ✅

**Реалізовано в [basics.md § 3](basics.md#3-локальне-середовище):**

**Два підходи:**

**Нові проєкти (bootstrap.sh):**
```yaml
volumes:
  - ./wp/wp-content:/var/www/html/wp-content
  - ./wp-config-local.php:/var/www/html/wp-config.php:ro
```
WP Core береться з Docker image.

**Міграція існуючих:**
```yaml
volumes:
  - ./wp:/var/www/html:cached
  - ./php.ini:/usr/local/etc/php/conf.d/custom.ini:ro
```
Монтуємо весь wp/ (включно з Core).

**Trade-offs документовані.**

**Статус:** ✅ Повністю вирішено

---

## 6. Rollback procedures відсутні

### Проблема

**Серйозність:** 🔴 HIGH

SOP не описує що робити якщо deploy зламав production.

**Вплив:**
- Паніка при проблемах
- Downtime на production
- Можлива втрата даних

### Рішення ✅

**Реалізовано в [migration.md § Rollback Plan](migration.md#rollback-plan):**

**3 рівні rollback:**

**Level 1: Quick Rollback (файли)**
```bash
Plesk File Manager → Restore з backup
```

**Level 2: Git Rollback**
```bash
git revert HEAD
git push origin main
Plesk → Git → Deploy
```

**Level 3: Full Rollback (Files + DB)**
```bash
Plesk Backup Manager → Restore Full Backup
```

**Превентивні заходи:**
- Checklist перед кожним deploy
- Backup strategy (3 рівні)
- Rollback drill (практика)

**Статус:** ✅ Повністю вирішено

---

## 7. Language files в .gitignore

### Проблема

**Серйозність:** 🟢 LOW

Bootstrap.sh виключає `wp/wp-content/uploads/` але не згадує `wp/wp-content/languages/`.

**Вплив:**
- Languages files потрапляють в Git
- Роздування репозиторію (сотні MB)
- Slower clones

### Рішення ✅

**Реалізовано в .gitignore template:**

```gitignore
# Language files (auto-downloaded by WordPress)
wp/wp-content/languages/
wp/wp-content/languages/plugins/
wp/wp-content/languages/themes/
```

**Додано в [basics.md § 4.2](basics.md#42-що-заборонено-в-git):**

```markdown
❌ НЕ в Git:
- wp/wp-content/languages/ (auto-downloaded by WordPress)
```

**Коментар в SOP чому це важливо.**

**Статус:** ✅ Повністю вирішено

---

## 8. Elementor CSS regeneration

### Проблема

**Серйозність:** 🟡 MEDIUM

При зміні структури файлів (особливо `themes/`) Elementor CSS ламається. SOP це не згадує.

**Вплив:**
- Сайт без стилів після deploy
- Плутанина "чому не працює?"
- Втрата часу на debugging

### Рішення ✅

**Реалізовано в [deployment.md § 8](deployment.md#8-post-deploy-checklist):**

**Post-Deploy Checklist:**
```markdown
- [ ] Якщо Elementor: Regenerate CSS & Data
- [ ] Hard refresh браузера (Ctrl+Shift+R)
```

**Інструкція:**
1. WP Admin → Elementor → Tools
2. Regenerate CSS & Data → "Regenerate Files"
3. Hard refresh: Cmd+Shift+R (Mac) або Ctrl+Shift+R (Win)

**Коли потрібно:**
- Після зміни theme structure
- Після оновлення Elementor
- Після deploy зі зміною paths
- Якщо стилі зламані

**Автоматизація (опційно):**
```bash
wp elementor flush-css
```

**Статус:** ✅ Повністю вирішено

---

## 9. Child theme paths при міграції

### Проблема

**Серйозність:** 🟡 MEDIUM

Child theme може використовувати абсолютні шляхи або посилання на `../wordpress/`. При міграції це ламається.

**Вплив:**
- Зламані стілі/scripts
- 404 на assets
- Потрібен manual fix

### Рішення ✅

**Реалізовано в [migration.md § Common Issues](migration.md#common-issues):**

**Phase 3.5: Check Child Theme Paths**

```bash
# Шукати hardcoded paths
grep -r "wordpress/" wp/wp-content/themes/[child-theme]/

# Шукати absolute URLs
grep -r "localhost:8080/wordpress" wp/wp-content/themes/[child-theme]/

# Перевірити functions.php
cat wp/wp-content/themes/[child-theme]/functions.php | grep -E "(get_stylesheet_directory|get_template_directory)"
```

**Якщо знайдено:**
1. Замінити `wordpress/` на `wp/`
2. Використовувати WP functions (не hardcoded):
   - `get_stylesheet_directory()` - child theme path
   - `get_template_directory()` - parent theme path
   - `get_stylesheet_directory_uri()` - child theme URL

**Статус:** ✅ Повністю вирішено

---

## 10. Staging environment strategy

### Проблема

**Серйозність:** 🟢 MEDIUM

SOP не згадує staging environment, хоча це best practice.

**Вплив:**
- Ризиковані deploys напряму на production
- Неможливість протестувати перед launch
- Більше downtime при проблемах

### Рішення ⚠️

**Рекомендації додано в документацію:**

**Option A: Plesk Subdomain**
```
Production: https://domain.com
Staging:    https://staging.domain.com
```

**Option B: Local Docker (Мінімум)**
```
Production: https://domain.com
Staging:    http://localhost:PORT
```

**Option C: Separate Server** (для великих проектів)

**Workflow з Staging:**
```
Local → Staging → Production
  ↓         ↓          ↓
 Test    Test+Demo   Live
```

**Best Practice:**
1. Feature branch → Staging deploy
2. Тестування на staging
3. Merge to main → Production deploy

**Статус:** ⚠️ Рекомендації додані, но не mandatory (залежить від бюджету)

---

## Підсумок

### Що спрацювало ✅

1. **Модульна структура SOP** — легко навігувати
2. **Phases 0-6 локально** — безпечна міграція
3. **Git mv** зберіг історію при `wordpress/` → `wp/`
4. **Manual Plesk mode** — контроль кожного deploy
5. **3-рівневий backup** — Git, Local DB, Production

### Що не спрацювало ❌

1. **README.md для людей** — не потрібен для ШІ-only workflow
2. **Багато SOP файлів** — плутанина між v2, improvements, quick ref
3. **Без автодокументації** — ШІ не оновлював PROJECT.md автоматично
4. **Технічні файли в root** — засмічували структуру
5. **Старі файли залишались** — потрібна була команда міграції

### Нові правила для ШІ 🤖

**ШІ ЗОБОВ'ЯЗАНИЙ:**

1. **Автоматично оновлювати PROJECT.md:**
   - Після додавання Redis/нових сервісів → Tech Stack
   - Після значних змін → Changelog
   - Після змін структури → Structure

2. **Готувати детальні commit messages:**
   ```
   feat(redis): Add Redis cache for performance

   - Added redis service to docker-compose.yml
   - Configured WP Redis plugin
   - Updated environment variables

   See PROJECT.md changelog for details
   ```

3. **Документувати КОЖНУ зміну:**
   - Не припускати що власник знає що змінилось
   - PROJECT.md — це єдина база знань

4. **STOP-RULE:** Якщо щось незрозуміло → запитати

---

## Recommendations для майбутніх проектів

### 1. Використовуй модульну SOP структуру

```
SOP.md (навігатор)
├─→ docs/sop/basics.md
├─→ docs/sop/deployment.md
├─→ docs/sop/migration.md
└─→ docs/sop/improvements.md
```

### 2. ШІ завжди оновлює PROJECT.md

- После додавання інструментів
- Після структурних змін
- Після важливих рішень

### 3. README.md мінімальний

Для ШІ-only workflow:
```markdown
# project-name

See PROJECT.md for details.
See CLAUDE.md for AI instructions.
```

### 4. Технічні файли в docs/

```
Root (тільки важливе):
├── CLAUDE.md
├── PROJECT.md
├── SERVER_RULES.md
├── docker-compose.yml
└── index.php

docs/ (технічне):
├── sop/
├── migration/
├── scripts/
└── archive/
```

### 5. .gitignore шаблон

Обов'язково виключити:
- `wp/wp-content/uploads/`
- `wp/wp-content/languages/`
- `wp/wp-content/plugins/*` (окрім custom-*)
- `backups/`
- `docs/archive/`
- `.env`

### 6. Manual Plesk deploy спочатку

- Mode: Manual (не Automatic)
- Контроль кожного deploy
- Після 1-2 днів → можна Automatic

### 7. Backup strategy (3 рівні)

1. **Git:** tags + branches
2. **Local DB:** dumps з timestamp
3. **Production:** Plesk backup + DB export

---

## Template Checklist для нових міграцій

**Перед початком:**
- [ ] Прочитав [migration.md](migration.md)
- [ ] Створив MIGRATION_AUDIT.md
- [ ] Створив MIGRATION_PLAN.md
- [ ] Зробив backup (3 рівні)

**Під час:**
- [ ] Phases 0-6 виконані локально
- [ ] Git зберіг історію (git mv використано)
- [ ] Local testing passed
- [ ] PROJECT.md оновлено

**Перед production:**
- [ ] Plesk Git налаштовано (Manual mode)
- [ ] Production backup створено
- [ ] Rollback plan зрозумілий
- [ ] Власник підтвердив

**Після production:**
- [ ] Elementor CSS regenerated
- [ ] Всі сторінки працюють
- [ ] Monitoring 24-48h
- [ ] PROJECT.md → Changelog updated

---

## Додаткові ресурси

- **[basics.md](basics.md)** — Git, Docker, структура
- **[deployment.md](deployment.md)** — Plesk, deploy, MODE, DB
- **[migration.md](migration.md)** — міграція старих проектів
- **[docs/migration/MIGRATION.md](../migration/MIGRATION.md)** — 68-стор. посібник

---

**Версія:** 2.1
**Базується на:** bsahlen.de migration (2026-01-28)
**Останнє оновлення:** 2026-01-28
