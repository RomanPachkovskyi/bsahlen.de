# SOP v2.0 IMPROVEMENTS

**Виявлені недоліки, пропуски та рекомендації для SOP v2.0**

**Базується на:** Реальна міграція проєкту bsahlen.de (2026-01-28)
**Автор:** Migration Analysis Team
**Версія:** 1.0

---

## Огляд

Під час міграції проєкту bsahlen.de зі старої структури на SOP v2.0 виявлено 10 критичних недоліків та пропусків у документації. Цей документ містить детальний аналіз кожної проблеми та рекомендації для покращення.

---

## 1. Відсутність інструкцій для міграції існуючих проектів

### Проблема

**Серйозність:** 🔴 CRITICAL

SOP v2.0 описує тільки створення НОВИХ проектів через `bootstrap.sh`. Немає жодної інформації про міграцію існуючих проектів.

**Вплив:**
- Неможливо мігрувати старі проєкти без додаткової документації
- Ризик зламати існуючі production сайти
- Відсутній rollback план

### Рішення

**Додати до SOP v2.0:**

1. Новий розділ **"16. Міграція існуючих проектів"**
2. Посилання на `MIGRATION.md` (загальний посібник)
3. Короткий огляд процесу міграції:
   ```
   Existing Project → Audit → Plan → Migrate Local → Test → Deploy
   ```

**Рекомендації:**
- Створити окремий документ `MIGRATION.md` (template)
- Додати до bootstrap.sh прапорець `--migrate-mode`
- Включити migration checklist в SOP

---

## 2. Неясна обробка 3rd party плагінів

### Проблема

**Серйозність:** 🟡 MEDIUM

SOP каже "тільки custom-* в Git", але не пояснює:
- Як саме визначити які плагіни custom?
- Як працювати з plugins/ якщо частина в Git, частина ні?
- Що робити з плагінами що мають customizations?

**Вплив:**
- Плутанина при розробці
- Можливі конфлікти при deploy
- Різні підходи в різних проектах

### Рішення

**Додати до розділу "4.1 Що зберігається в Git":**

```markdown
### Плагіни в Git: Детальні правила

**✅ В Git:**
- `wp/wp-content/plugins/custom-*` - будь-які custom плагіни
- Плагіни створені студією з нуля
- Приватні плагіни (недоступні в WP repo)

**❌ НЕ в Git:**
- Публічні плагіни з wordpress.org
- Преміум плагіни (Elementor Pro, ACF Pro, etc.)
- Будь-які плагіни що можна встановити через WP Admin

**⚠️ Особливі випадки:**
- Якщо 3rd party плагін має customizations → fork + rename до custom-*
- Якщо плагін недоступний (deprecated) → обговорити з командою
```

**Рекомендації:**
- .gitignore має містити явні виключення для всіх 3rd party
- Додати в PROJECT.md секцію "Plugin Management Strategy"

---

## 3. Відсутність інструкцій для Plesk Git setup

### Проблема

**Серйозність:** 🔴 HIGH

SOP згадує "Plesk auto-deploy" але немає покрокової інструкції:
- Як налаштувати Git в Plesk?
- Які саме параметри встановити?
- Як згенерувати і додати SSH keys?
- Що робити якщо щось не працює?

**Вплив:**
- Власник проєкту не може налаштувати deploy
- Ризик неправильної конфігурації
- Можливий downtime на production

### Рішення

**Додати до розділу "6.2 Plesk Git налаштування":**

```markdown
### Покрокова інструкція Plesk Git Setup

**Крок 1: Увійти в Plesk**
- URL: https://plesk.yourdomain.com:8443
- Domains → [your-domain] → Git

**Крок 2: Налаштувати Repository**
1. Клікнути "Manage"
2. Repository URL: `https://github.com/user/project.git`
3. Repository path: `/httpdocs` (корінь)
4. Branch to deploy: `main`

**Крок 3: Deployment Mode**
⚠️ ВАЖЛИВО: Спочатку MANUAL!
- Mode: **Manual** (не Automatic!)
- Причина: контрольований deploy, без сюрпризів

**Крок 4: SSH Keys (якщо приватний repo)**
1. Plesk → Generate Key Pair
2. Копіювати Public Key
3. GitHub → Settings → Deploy keys → Add deploy key
4. Title: "Plesk [domain]"
5. Key: [paste public key]
6. ✓ Allow write access: NO (тільки read!)

**Крок 5: Тестовий Pull**
1. Plesk → Git → "Pull Updates"
2. Перевірити Output log:
   - "Successfully pulled" → ✅ OK
   - Errors → перевірити URL, SSH keys
3. **НЕ натискати Deploy!** (поки не готові)

**Крок 6: Deploy Actions (налаштування)**
Plesk → Git → Deploy Actions:
- Run script: [empty] (поки не потрібно)
- Additional commands: [empty]

**Крок 7: Перший Manual Deploy**
1. Backup production (Files + DB!)
2. Plesk → Git → "Deploy"
3. Перевірити Output log
4. Перевірити сайт: https://domain.com

**Крок 8: Переключення на Automatic (опційно)**
Після 1-2 днів стабільної роботи:
- Plesk → Git → Settings
- Mode: Manual → **Automatic**
- Тепер кожен push автоматично деплоїться
```

**Рекомендації:**
- Скріншоти кожного кроку
- Troubleshooting секція для поширених помилок
- Відео-інструкція (опційно)

---

## 4. Неясності з MODE='maintenance' для живих сайтів

### Проблема

**Серйозність:** 🟡 MEDIUM

SOP каже що MODE='maintenance' за замовчуванням для нових проектів. Але що якщо сайт УЖЕ живий і мігрується на SOP v2.0?

**Вплив:**
- Ризик випадкового вимкнення живого сайту
- Плутанина з default MODE
- Можливий SEO impact (якщо сайт показує maintenance)

### Рішення

**Додати до розділу "7.1 Два режими":**

```markdown
### Default MODE залежно від Project State

**Нові проєкти (bootstrap.sh):**
- Default: `MODE = 'maintenance'`
- Причина: сайт ще не готовий до публікації

**Існуючі проєкти (міграція):**
- Якщо сайт живий → `MODE = 'live'`
- Якщо сайт в розробці → `MODE = 'maintenance'`

**⚠️ ВАЖЛИВО:**
При міграції живого сайту завжди встановлюй:
```php
define('MODE', 'live'); // НЕ 'maintenance'!
```

**Перевірка перед deploy:**
1. Прочитати PROJECT.md → Project State
2. Якщо State = LIVE → MODE має бути 'live'
3. Подвійна перевірка index.php перед push
```

**Рекомендації:**
- Додати в MIGRATION.md явну інструкцію про MODE
- В PROJECT.md мати поле "Default MODE"

---

## 5. Docker mount strategy для міграції

### Проблема

**Серйозність:** 🟡 MEDIUM

Bootstrap.sh монтує тільки `/wp/wp-content`, але при міграції безпечніше монтувати весь `/wp` (щоб WP Core теж був локально).

**Вплив:**
- Плутанина з mount strategy
- Можливі проблеми з permissions
- Складніше дебажити

### Рішення

**Додати до розділу "3. Локальне середовище":**

```markdown
### Docker Mount Strategy

**Нові проєкти (bootstrap.sh):**
```yaml
volumes:
  - ./wp/wp-content:/var/www/html/wp-content
  - ./wp-config-local.php:/var/www/html/wp-config.php:ro
```
WP Core береться з Docker image.

**Міграція існуючих проектів:**
```yaml
volumes:
  - ./wp:/var/www/html:cached
  - ./php.ini:/usr/local/etc/php/conf.d/custom.ini:ro
```
Монтуємо весь wp/ (включно з Core).

**Причина:**
- Зберігаємо існуючий WP Core
- Менше ризику при міграції
- Простіше дебажити

**Після стабілізації:**
Можна перейти на mount тільки wp-content (як в bootstrap).
```

**Рекомендації:**
- Явно документувати обидва підходи
- Пояснити trade-offs кожного

---

## 6. Відсутність rollback procedures

### Проблема

**Серйозність:** 🔴 HIGH

SOP не описує що робити якщо deploy зламав production.

**Вплив:**
- Паніка при проблемах
- Downtime на production
- Можлива втрата даних

### Рішення

**Додати новий розділ:**

```markdown
## X. Emergency Rollback Procedures

### Коли використовувати

Rollback потрібен якщо:
- Production сайт не працює після deploy
- Критичні баги виявлені на production
- Структурні зміни зламали функціонал

### Quick Rollback (Git)

**Якщо проблема в коді:**

1. Локально:
```bash
git checkout backup-pre-migration
# або
git revert HEAD~X  # X = кількість комітів назад
```

2. Push:
```bash
git push origin main --force
```
⚠️ Force push тільки в аварійній ситуації!

3. Plesk:
```
Plesk → Git → Pull Updates → Deploy
```

### Full Rollback (Files + DB)

**Якщо Git rollback не допоміг:**

1. **Зупинити трафік (опційно):**
   - Cloudflare → Enable "I'm Under Attack Mode"
   - Або створити placeholder index.html

2. **Відновити файли:**
   - Plesk → File Manager
   - Видалити `/httpdocs/*`
   - Відновити з Backup (Plesk Backup Manager)

3. **Відновити БД:**
   - Plesk → Databases → Import
   - Вибрати бекап SQL файл

4. **Перевірити:**
   ```bash
   curl -I https://domain.com
   # Має бути: HTTP/2 200
   ```

5. **Логування:**
   - Задокументувати що пішло не так
   - Записати в PROJECT.md → Changelog

### Превентивні заходи

**Перед кожним production deploy:**
- ✅ Бекап БД (< 1 година назад)
- ✅ Бекап файлів (Plesk Backup)
- ✅ Тестування локально
- ✅ Rollback plan готовий
- ✅ Є вільний час на фікс (1-2 години)
```

**Рекомендації:**
- Drill rollback procedure (практикувати)
- Мати monitoring/alerting
- Post-mortem після кожного rollback

---

## 7. Мовні файли не згадані явно в .gitignore

### Проблема

**Серйозність:** 🟢 LOW

Bootstrap.sh виключає `wp/wp-content/uploads/` але не згадує `wp/wp-content/languages/`.

**Вплив:**
- Languages files потрапляють в Git
- Роздування репозиторію (сотні MB)
- Slower clones

### Рішення

**Оновити .gitignore template в bootstrap.sh:**

```gitignore
# Додати:

# Language files (auto-downloaded by WordPress)
wp/wp-content/languages/
wp/wp-content/languages/plugins/
wp/wp-content/languages/themes/
```

**Також додати коментар в SOP:**

```markdown
## Важливо: Language Files

WordPress автоматично завантажує мовні файли при оновленні.
Вони НЕ мають бути в Git.

Виключити в .gitignore:
- `wp/wp-content/languages/`
```

---

## 8. Elementor CSS regeneration не згадано

### Проблема

**Серйозність:** 🟡 MEDIUM

При зміні структури файлів (особливо themes/) Elementor CSS ламається. SOP це не згадує.

**Вплив:**
- Сайт без стилів після deploy
- Плутанина "чому не працює?"
- Втрата часу на debugging

### Рішення

**Додати до кількох місць в SOP:**

**1. Розділ "10. Дозволене на продакшні":**

```markdown
### After Structural Changes

Якщо змінювалась структура файлів:
1. wp-admin → Elementor → Tools
2. Regenerate CSS & Data → "Regenerate Files"
3. Hard refresh: Cmd+Shift+R (Mac) або Ctrl+Shift+R (Win)

**Коли потрібно:**
- Після зміни theme structure
- Після оновлення Elementor
- Після deploy зі зміною paths
- Якщо стилі зламані
```

**2. Checklist: Go-Live:**

```markdown
- [ ] Elementor CSS regenerated
```

**Рекомендації:**
- Зробити це частиною deploy checklist
- Автоматизувати через WP-CLI (якщо можливо):
  ```bash
  wp elementor flush-css
  ```

---

## 9. Child theme compatibility при зміні структури

### Проблема

**Серйозність:** 🟡 MEDIUM

Child theme може використовувати абсолютні шляхи або посилання на `../wordpress/`. При міграції це ламається.

**Вплив:**
- Зламані стилі/scripts
- 404 на assets
- Потрібен manual fix

### Рішення

**Додати до MIGRATION.md:**

```markdown
### Phase 3.5: Check Child Theme Paths

Після rename wordpress/ → wp/:

```bash
# Шукати hardcoded paths
grep -r "wordpress/" wp/wp-content/themes/[child-theme]/

# Шукати absolute URLs
grep -r "localhost:8080/wordpress" wp/wp-content/themes/[child-theme]/

# Перевірити functions.php
cat wp/wp-content/themes/[child-theme]/functions.php | grep -E "(get_stylesheet_directory|get_template_directory)"
```

**Якщо знайдено:**
1. Замінити `wordpress/` на `wp/`
2. Переконатись що використовуються WP functions (не hardcoded paths):
   - `get_stylesheet_directory()` - child theme path
   - `get_template_directory()` - parent theme path
   - `get_stylesheet_directory_uri()` - child theme URL
```

**Рекомендації:**
- Додати automated check script
- Включити в Phase 5 testing checklist

---

## 10. Відсутність staging environment strategy

### Проблема

**Серйозність:** 🟡 MEDIUM

SOP не згадує staging environment, хоча це best practice.

**Вплив:**
- Ризиковані deploys напряму на production
- Неможливість протестувати перед launch
- Більше downtime при проблемах

### Рішення

**Додати новий розділ:**

```markdown
## Y. Staging Environment (Recommended)

### Переваги

- Тестування перед production
- Демо для клієнта
- Менше ризику
- Safe experimentation

### Setup Options

**Option A: Plesk Subdomain**
```
Production: https://domain.com
Staging:    https://staging.domain.com
```

Plesk:
1. Додати subdomain `staging`
2. Клонувати Git repo (окрема deploy)
3. Копіювати БД → URL replacement

**Option B: Local Docker (Мінімум)**
```
Production: https://domain.com
Staging:    http://localhost:PORT
```

Docker локально з production DB (copies).

**Option C: Separate Server**
Для великих проектів.

### Workflow з Staging

```
Local → Staging → Production
  ↓         ↓          ↓
 Test    Test+Demo   Live
```

**Best Practice:**
1. Feature branch → Staging deploy
2. Тестування на staging
3. Merge to main → Production deploy
```

**Рекомендації:**
- Якщо бюджет дозволяє - завжди staging
- Мінімум: локальне тестування з production DB copy

---

## Підсумок по пріоритетах

### 🔴 CRITICAL (мають бути в SOP v2.1)

1. **Міграція існуючих проектів** (розділ 16)
2. **Plesk Git setup** (детальна інструкція)
3. **Rollback procedures** (новий розділ)

### 🟡 HIGH (дуже бажано)

4. **3rd party plugins** (чіткі правила)
5. **MODE для живих сайтів** (clarification)
6. **Elementor CSS regeneration** (checklist items)
7. **Child theme paths** (migration guide)

### 🟢 MEDIUM (покращення)

8. **Docker mount strategy** (2 підходи)
9. **Language files** (.gitignore template)
10. **Staging environment** (best practice)

---

## Рекомендації для SOP v2.1

### Структурні зміни

1. **Додати розділи:**
   - 16. Міграція існуючих проектів
   - 17. Rollback & Emergency Procedures
   - 18. Staging Environment Strategy

2. **Розширити існуючі:**
   - 4.1 → детальні правила для plugins
   - 6.2 → покрокова Plesk інструкція
   - 7.1 → MODE залежно від Project State

3. **Додати чеклисти:**
   - Pre-migration checklist
   - Deploy checklist (з Elementor CSS)
   - Post-deploy validation checklist

### Документація

**Нові документи:**
- `MIGRATION.md` (template для міграції)
- `TROUBLESHOOTING.md` (поширені проблеми)
- `ROLLBACK.md` (emergency procedures)

**Оновити:**
- `bootstrap.sh` (.gitignore template + languages)
- `SOP_v2.md` (всі improvements)

### Тестування

Протестувати SOP v2.1 на:
- [ ] Новий проєкт (bootstrap.sh)
- [ ] Міграція legacy WordPress
- [ ] Міграція з різними хостингами (не тільки Plesk)
- [ ] Rollback scenario

---

## Висновки

Міграція bsahlen.de виявила що SOP v2.0 **чудово працює для нових проектів**, але потребує значного доопрацювання для міграції існуючих.

**Ключові takeaways:**
1. Завжди потрібен migration guide
2. Rollback план критично важливий
3. Plesk setup потребує детальних інструкцій
4. Чеклисти запобігають помилкам

**Наступні кроки:**
1. Імплементувати CRITICAL improvements → SOP v2.1
2. Створити templates (MIGRATION.md, ROLLBACK.md)
3. Протестувати на 2-3 проектах
4. Зібрати feedback → SOP v2.2

---

**Документ створено:** 2026-01-28
**Базується на:** Реальна міграція bsahlen.de
**Статус:** ✅ Готовий до review
**Версія:** 1.0
